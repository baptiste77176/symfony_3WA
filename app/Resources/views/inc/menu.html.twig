<div class="container">
    <div class="row">
        <div class="col-xs-12">
            <ul class="nav nav-pills">
                {% for result in result %}

                    <li><a href="{{ url('formation.info',{'id' :result.id}) }}" >{{ result.name }}</a></li>

                {% endfor %}



            </ul>
        </div>
    </div>